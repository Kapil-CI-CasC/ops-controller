removeStrategy:
  rbac: SYNC
  items: NONE
items:
- kind: folder
  name: kapil-ci-casc
  groups:
  - members:
      users:
      - kmalik4
      - kmalik4-admin
    roles:
    - name: browse
      grantedAt: current
    - name: workshop-admin
      grantedAt: child
    name: Team Administrators
  filteredRoles:
  - workshop-admin
  - browse
  items:
  - kind: managedController
    name: ops-controller
    properties:
    - healthReporting:
        enabled: true
    - configurationAsCode:
        bundle: kapil-ci-casc-ops-controller
    configuration:
      kubernetes:
        memory: 4000
        cpus: 1.0
        clusterEndpointId: default
        disk: 10
        storageClassName: premium-rwo
        domain: kapil-ci-casc-ops-controller
        namespace: controllers
        yaml: |
          kind: "StatefulSet"
          spec:
            template:
              metadata:
                labels:
                  networking/allow-internet-access: "true"
              spec:
                containers:
                - name: "jenkins"
                  env:
                  - name: "SECRETS"
                    value: "/var/jenkins_home/jcasc_secrets"
                  - name: "GITHUB_ORGANIZATION"
                    value: "Kapil-CI-CasC"
                  - name: "GITHUB_USER"
                    value: "kmalik4"
                  - name: "GITHUB_APP"
                    value: "cloudbees-ci-casc-workshop"
                  - name: "CONTROLLER_SUBDOMAIN"
                    value: "kapil-ci-casc-ops-controller"
                  - name: "CASC_BUNDLE_ID"
                    value: "kapil-ci-casc-ops-controller"
                  volumeMounts:
                  - name: "jcasc-secrets"
                    mountPath: "/var/jenkins_home/jcasc_secrets"
                volumes:
                - name: "jcasc-secrets"
                  csi:
                    driver: secrets-store.csi.k8s.io
                    readOnly: true
                    volumeAttributes:
                      secretProviderClass: "cbci-mc-secret-provider"
